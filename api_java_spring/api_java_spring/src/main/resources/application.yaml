server:
  port: 8080
  servlet:
    context-path: /identity

spring:
  datasource:
    url: "jdbc:mysql://127.0.0.1:3306/identity_service?useSSL=false&serverTimezone=UTC"
    username: root
    password: 12345
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


